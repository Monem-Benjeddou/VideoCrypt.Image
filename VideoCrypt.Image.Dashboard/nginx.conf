worker_processes auto;

error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    keepalive_timeout 65;

    include /etc/nginx/conf.d/*.conf;

    upstream imagecrypt_caching_app {
        server imagecrypt-caching-app:4000;
    }

    upstream imagecrypt_api {
        server imagecrypt-api:7003;
    }

    server {
        listen 80;
        server_name api.john-group.org;

        location / {
            proxy_pass http://imagecrypt_caching_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 80;
        server_name api.john-group.org;

        location /dynamic/ {
            set $upstream_port 4001; 
            if ($uri ~ "^/dynamic/([0-9]+)") {
                set $upstream_port $1;
            }
            proxy_pass http://imagecrypt_api:$upstream_port;
        }

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
