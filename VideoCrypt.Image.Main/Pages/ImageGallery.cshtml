@page "/ImageGallery/{currentPage}"
@using Hydro
@using VideoCrypt.Image.Main.Pages.Components
@model ImageGallery


<div>
    @(await Component.Hydro<ImagesGalleryComponent>(new { CurrentPage = Model.CurrentPage }))
</div>
